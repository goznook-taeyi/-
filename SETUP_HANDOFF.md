# 설치 인수인계 — 콘텐츠 기획 플레이북 v2 배포

이 문서는 **회원님 PC의 Claude Code(로컬)** 가 읽고 그대로 실행하기 위한 설치 지시서다.
사용자에게는 결과 요약만 보고하고, 아래 작업은 스스로 진행한다. (파괴적 작업 전엔 반드시 백업)

## 목표

이 저장소의 `PLAYBOOK_V2.md`(콘텐츠 기획 정본)를 PC의 기획 도구 3곳에 배치해서,
어떤 도구로 기획하든 **같은 팀 구조(팀장 단독 기획 + 후킹 대표 리뷰) · 같은 6열 양식**이 나오게 만든다.

## 작업 순서

### 1. 최신 플레이북 확보
이 저장소(`goznook-taeyi/-`)의 브랜치 `claude/auto-agent-setup-yesvsr`에서 `PLAYBOOK_V2.md`를 가져온다.

### 2. 키티에 설치 (가장 중요)
- 대상: `C:\ai-projects\shortform\`
- 기존 `CLAUDE.md`가 있으면 `CLAUDE.md.bak_YYYYMMDD`로 백업 후, `PLAYBOOK_V2.md` 내용을 `CLAUDE.md`로 저장한다.
- `mascot.py`를 열어 `claude -p`(또는 claude.exe) 호출 부분을 확인한다:
  - **작업 디렉토리가 `C:\ai-projects\shortform`인 상태로 호출되는지** 확인 (그래야 CLAUDE.md가 자동 로드됨). 아니라면 cwd를 맞추거나 호출 프롬프트에 CLAUDE.md를 읽으라는 지시를 추가한다.
  - 호출 프롬프트 문자열 안에 **자체 기획안 양식이 하드코딩**되어 있으면, 그 양식 부분을 제거하고 "CLAUDE.md의 플레이북 v2를 따르라"로 교체한다. (양식 충돌의 원인 제거)
- 구글시트 생성 로직이 있다면 열 구성이 6열(no/촬영장소/상단질문/후킹멘트/Main/CTA)과 일치하는지 확인하고, 다르면 6열로 맞춘다. **원장용 촬영본 시트(또는 탭)** 도 함께 생성하도록 할 수 있으면 추가한다 (양식은 PLAYBOOK_V2.md §4-1).

### 3. 바탕화면 '콘텐츠기획팀' 폴더 파악 + 설치
- `C:\Users\2taey\Desktop\콘텐츠기획팀` (또는 OneDrive 바탕화면일 수 있음 — `[Environment]::GetFolderPath('Desktop')`로 확인)
- **폴더 안에 뭐가 있는지 목록과 각 파일의 정체를 파악해서 사용자에게 보고한다.** (어느 도구가 만든 것인지 추정 포함 — Codex가 만든 것일 가능성 있음)
- `PLAYBOOK_V2.md`를 이 폴더에 복사한다.
- 폴더 안에 `AGENTS.md`(Codex 지침)나 `CLAUDE.md`가 이미 있으면: 백업 후 플레이북 v2 내용으로 교체한다. 없고 이 폴더가 Codex 작업 폴더로 보이면 `AGENTS.md`라는 이름으로도 저장한다.
- 폴더에 기존 기획 산출물이 있으면, 다룬 주제를 추려 PLAYBOOK의 §7(반복 방지 목록)에 추가한다.

### 4. Codex 대응
- PC에서 Codex(OpenAI) 작업 폴더를 쓰는 것으로 보이면(3번 폴더 포함), 해당 폴더에 `AGENTS.md`로 동일 내용을 저장한다.

### 5. 검수 (완료 판정 기준)
1. `C:\ai-projects\shortform`에서 키티(또는 `claude -p`)에게 **"코비쥬 신규 기획 3개 뽑아줘"** 를 실행한다.
2. 결과가 다음을 만족하면 성공:
   - 6열 표(no/촬영장소/상단질문/후킹멘트/Main/CTA) + **원장용 촬영본 표** 2개가 출력됨
   - PLAYBOOK §7의 기존 주제 15개와 겹치지 않음
   - 프로세스가 "팀장 초안 → 대표 후킹 리뷰 → 반영" 순서로 표시됨
3. 실패하면 원인을 수정하고 재시도한다.

### 6. 보고
사용자에게 다음을 한국어로 짧게 보고한다:
- 설치한 위치 3곳과 백업 파일명
- '콘텐츠기획팀' 폴더의 정체(안에 뭐가 있었는지)
- 검수 결과(성공한 기획 3개 표 포함)
- mascot.py를 수정했다면 무엇을 바꿨는지

## 참고
- 이 설치의 배경: 여러 도구(키티/Codex/클라우드 세션)가 각자 다른 양식으로 기획을 만들어 충돌이 났다. PLAYBOOK_V2.md 하나를 정본으로 삼아 전부 통일하는 것이 목적이다.
- 팀 구조가 v1(다인 교차검증)에서 v2(팀장 단독+대표 리뷰)로 바뀌었다. 로컬에 v1 흔적(여러 에이전트가 서로 비평하는 설정)이 있으면 v2로 교체한다.
